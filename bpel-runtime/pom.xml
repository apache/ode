<?xml version="1.0"?>
<project>
    <groupId>pxe</groupId>
    <artifactId>pxe-bpel-runtime</artifactId>
    <name>PXE :: BPEL Runtime</name>

    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>pxe</groupId>
        <artifactId>pxe</artifactId>
        <version>2.0-SNAPSHOT</version>
    </parent>
    <version>2.0-SNAPSHOT</version>
    <dependencies>
        <dependency>
            <groupId>pxe</groupId>
            <artifactId>pxe-bpel-api</artifactId>
        </dependency>
        <dependency>
            <groupId>pxe</groupId>
            <artifactId>pxe-bpel-compiler</artifactId>
        </dependency>
        <dependency>
            <groupId>pxe</groupId>
            <artifactId>pxe-bpel-obj</artifactId>
        </dependency>
        <dependency>
            <groupId>pxe</groupId>
            <artifactId>pxe-jacob</artifactId>
        </dependency>
        <dependency>
            <groupId>pxe</groupId>
            <artifactId>pxe-utils</artifactId>
        </dependency>

        <dependency>
          <groupId>stax</groupId>
          <artifactId>stax-api</artifactId>
        </dependency>

        <dependency>
          <groupId>commons-collections</groupId>
          <artifactId>commons-collections</artifactId>
          <version>${commonsCollectionsVersion}</version>
        </dependency>

    </dependencies>
    <build>
        <plugins>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>xdoclet-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>xdoclet</goal>
                        </goals>
                        <configuration>
                            <tasks>
                                <doclet destDir="${project.build.directory}/generated-sources/xdoclet" verbose="true">
                                    <fileset dir="${basedir}/src/main/java" includes="**/*.java"/>
                                    <template havingClassTag="jacob.kind" destinationFile="{0}ML.java" templateFile="${basedir}/../jacob/src/xdt/com/fs/jacob/ML.xdt"/>
                                    <template havingClassTag="jacob.kind" destinationFile="{0}Channel.java" templateFile="${basedir}/../jacob/src/xdt/com/fs/jacob/channel.xdt"/>
                                </doclet>
                            </tasks>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
