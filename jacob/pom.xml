<?xml version="1.0"?>
<project>
    <groupId>ode</groupId>
    <artifactId>ode-jacob</artifactId>
    <name>ODE :: Java Concurrent Objects (JACOB)</name>

    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>ode</groupId>
        <artifactId>ode</artifactId>
        <version>2.0-SNAPSHOT</version>
    </parent>
    <version>2.0-SNAPSHOT</version>
    <dependencies>
        <dependency>
            <groupId>ode</groupId>
            <artifactId>ode-utils</artifactId>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>xdoclet-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>xdoclet</goal>
                        </goals>
                        <configuration>
                            <tasks>
                                <doclet destDir="${project.build.directory}/generated-sources/xdoclet" verbose="false">
                                    <fileset dir="${basedir}/src/main/java" includes="**/*.java"/>
                                    <template havingClassTag="jacob.kind" destinationFile="{0}ML.java" templateFile="${basedir}/src/xdt/com/fs/jacob/ML.xdt"/>
                                    <template havingClassTag="jacob.kind" destinationFile="{0}Channel.java" templateFile="${basedir}/src/xdt/com/fs/jacob/channel.xdt"/>
                                </doclet>
                            </tasks>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
